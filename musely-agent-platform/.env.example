# Platform LLM keys — set the same values in backend secrets (root .env or fly-staging/backend/secrets.env).
# Admin sync merges these into each user agent's /opt/data/.env
OPENROUTER_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
