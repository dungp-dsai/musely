# Local development — backend on Docker network + Docker socket for per-user agents.
# Frontend runs on the host (Vite HMR) via scripts/dev.sh.
#
# Usage: ./scripts/dev.sh

services:
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile.dev
    image: musely-backend:local
    container_name: musely-backend
    ports:
      - "8081:8081"
    volumes:
      - ./apps/backend:/app
      - backend_node_modules:/app/node_modules
      - ./data:/app/data
      - ./hermes-base:/opt/hermes-base:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - ./.env:/env/.env:ro
    environment:
      HERMES_ORCHESTRATOR: docker
      HERMES_IMAGE: musely-agent:local
      HERMES_NETWORK: musely-net
      HERMES_BASE_MOUNT: /opt/hermes-base
      HERMES_BASE_HOST_DIR: ${HERMES_BASE_HOST_DIR}
      DB_PATH: /app/data/musely.db
      HOST: 0.0.0.0
      PORT: 8081
    networks:
      - musely-net
    restart: unless-stopped

volumes:
  backend_node_modules:

networks:
  musely-net:
    name: musely-net
    external: true
