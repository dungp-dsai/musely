# Musely per-user Hermes agent — production
# Deploy from repo root:  fly deploy --config fly-prod/agent/fly.toml
app = "musely-prod-agent"
primary_region = "sin"

[build]
  dockerfile = "apps/agent/Dockerfile"

[env]
  API_SERVER_HOST = "0.0.0.0"
  API_SERVER_PORT = "8642"

# Secrets: copy secrets.env.example → secrets.env, then:
#   ./scripts/fly-secrets-import.sh fly-prod/agent

[[vm]]
  memory   = "256mb"
  cpu_kind = "shared"
  cpus     = 1
